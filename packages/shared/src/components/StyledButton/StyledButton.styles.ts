import type { SxProps, Theme } from '@mui/material/styles';

export const styledButtonStyles: Record<string, SxProps<Theme>> = {
    button: {
        borderRadius: '30px',
        padding: '10px 20px',
        width: '100%',
        height: '100%',
        fontSize: 16,
        fontWeight: 600,
        textTransform: 'none',
    }
  };
  