import type { SxProps, Theme } from '@mui/material/styles';

export const orderStatusChipStyles: Record<string, SxProps<Theme>> = {
  chip: {
    fontWeight: 500,
    textTransform: 'capitalize',
  },
};
