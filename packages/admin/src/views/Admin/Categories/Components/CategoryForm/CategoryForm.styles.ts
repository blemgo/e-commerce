import type { SxProps, Theme } from "@mui/material/styles";

export const categoryFormStyles: Record<string, SxProps<Theme>> = {
  content: {
    display: "flex",
    flexDirection: "column",
    gap: 2,
    pt: 1,
  },
};
