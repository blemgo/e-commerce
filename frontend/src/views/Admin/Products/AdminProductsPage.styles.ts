import type { SxProps, Theme } from "@mui/material/styles";

export const adminProductsPageStyles: Record<string, SxProps<Theme>> = {
  searchContainer: {
    position: "relative",
    mb: 3,
  },
  categoryBar: {
    mb: 2,
  },
};
